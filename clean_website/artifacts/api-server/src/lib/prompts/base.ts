// Common formatting instructions shared by every content category. Anything
// that isn't specific to a category (vocabulary, STEM, programming, legal,
// historical) belongs here -- the dispatcher (index.ts) falls back to this
// module for the "General" path so there's exactly one copy of the default
// instructions instead of one per category.
export {};
